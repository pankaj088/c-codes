#include <stdio.h>

int main()
{
   int length, breath;
   printf("what is the length");
   scanf("%d", &length);
   printf("what is breath");
   scanf("%d", &breath);
   printf(" the are of rectangel %d", length * breath);

   return 0;
}