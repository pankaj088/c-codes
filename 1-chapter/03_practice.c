#include <stdio.h>

int main()
{
    int principle=100, rate =4, years =1;
    int simpleinterset = (principle *rate* years)/100;
    printf("the value of simple interset is %d",simpleinterset);
    return 0;
}