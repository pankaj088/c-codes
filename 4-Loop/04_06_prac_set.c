#include <stdio.h>

int main()
{
    int n, i = n-1;
    printf("Enter the number");
    scan("%d",&n);
    while (i<n)
        if(n%i == 0)
        {
            printf("prime number\n");
        }
        i--;
       if else
        {
            printf(" Not a prime no ");
        }
    
    
    return 0;
}