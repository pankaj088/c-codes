#include <stdio.h>

int main()
{
    // int a= 4; //type declreation instriction
    int a = 4, b, c; // type declreation instriction
    b = c = a;

    printf("the vale of a is %d\n", a);
    printf("the value of b is %d\n", b);
    printf("the value of c is %d\n", c);
    // float a= 2.1;
    // float b = a+8.9;
    // printf("the value of b is %f\n",b);

    return 0;
}